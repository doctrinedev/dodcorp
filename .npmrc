# Workspace packages resolve to each other on disk, never via the registry.
link-workspace-packages=true
# Peer deps are installed automatically; packages declare their own real deps.
auto-install-peers=true
