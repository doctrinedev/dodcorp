## API Report File for "@dodcorp/webhooks"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export const DEFAULT_TOLERANCE_SECONDS = 300;

// @public
export function isValidWebhookSignature(payload: WebhookPayload, header: string, secret: string, options?: VerifyOptions): boolean;

// @public (undocumented)
export interface SignOptions {
    timestamp?: number;
}

// @public
export function signWebhookPayload(payload: WebhookPayload, secret: string, options?: SignOptions): string;

// @public (undocumented)
export interface VerifyOptions {
    now?: number;
    toleranceSeconds?: number;
}

// @public
export function verifyWebhookSignature(payload: WebhookPayload, header: string, secret: string, options?: VerifyOptions): void;

// @public
export type WebhookPayload = string | Uint8Array;

// @public
export class WebhookVerificationError extends Error {
    constructor(code: WebhookVerificationErrorCode, message: string);
    // (undocumented)
    readonly code: WebhookVerificationErrorCode;
    // (undocumented)
    readonly name = "WebhookVerificationError";
}

// @public
export type WebhookVerificationErrorCode =
/** The signature header was absent, empty, or not in the expected format. */
'malformed_header'
/** The header parsed, but carried no signature of a scheme we understand. */
| 'no_supported_signature'
/** The timestamp is outside the accepted tolerance window (replay defence). */
| 'timestamp_out_of_tolerance'
/** The header parsed and was fresh, but no signature matched. */
| 'signature_mismatch';

// (No @packageDocumentation comment for this package)

```
