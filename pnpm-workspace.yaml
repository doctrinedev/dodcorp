packages:
  - 'packages/*'

# pnpm 10 blocks dependency install scripts by default. esbuild needs its
# postinstall to fetch the platform binary that tsup and vitest run on.
onlyBuiltDependencies:
  - esbuild
